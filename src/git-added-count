function git-added-count()
{
    git-in-repository; [[ $? == 0 ]] && {
        git status --porcelain 2>/dev/null | grep '^ *A' 2>/dev/null | wc -l 2>/dev/null | tr -d ' '
        return 0
    }

    return -1
}
