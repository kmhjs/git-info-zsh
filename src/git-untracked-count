function git-untracked-count()
{
    git-in-repository; [[ $? == 0 ]] && {
        git status --porcelain | grep '^ *??' 2>/dev/null | wc -l 2>/dev/null | tr -d ' '
        return 0
    }

    return -1
}
