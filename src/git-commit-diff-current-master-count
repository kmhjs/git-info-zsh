function git-commit-diff-current-master-count()
{
    git-in-repository; [[ $? == 0 ]] && {
        git-commit-diff-current-master | wc -l 2>/dev/null | tr -d ' '
        return $?
    }

    return -1
}
