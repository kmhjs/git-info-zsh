function git-commit-diff-current-master-count()
{
    if git-in-repository
    then
        local s
        s=($(git-commit-diff-current-master))

        echo ${#s}

        return $?
    fi

    return 1
}
