function git-commit-diff-current-master-count()
{
    git-in-repository; [[ $? == 0 ]] && {
        local s
        s=($(git-commit-diff-current-master)); echo ${#s}
        return $?
    }

    return -1
}
