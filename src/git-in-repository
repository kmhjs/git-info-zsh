function git-in-repository()
{
    if [[ -e ${GIT_RELATED_CMDS_SHARED_TMP} ]]
    then
      return 0
    fi

    if [[ -n "$(git branch 2>/dev/null)" ]]
    then
        return 0
    fi

    return 1
}
